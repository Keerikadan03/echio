// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  name String
  email String @unique
  password String
}

model Campaign {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  owner_id String
  name String
  description String
  image String?
  @@unique([owner_id, name])
  }

model Influencer {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  user_id String @unique
  followers Int
  avg_likes Int
  avg_comments Int
  }

model CampaignInfluencerPending {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  campaign_id String
  influencer_id String
  @@unique([campaign_id, influencer_id])
  }

model CampaignInfluencerAccepted {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  campaign_id String
  influencer_id String
  @@unique([campaign_id, influencer_id])
  }

